/// Polling engine — implements [`tc_engine_api::RoomEngine`] for poll-type rooms.
///
/// This struct will be filled in as poll-specific logic is extracted from `service/src/rooms/`.
pub struct PollingEngine;
